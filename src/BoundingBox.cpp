#include "BoundingBox.h"

// bbs=bbNms(bbs,pNms);
// still need to understand what is a PNms or how to place it better, then the name will be changed
void BoundingBox::nonMaximalSuppression(PNms pNms)
{
	
}
